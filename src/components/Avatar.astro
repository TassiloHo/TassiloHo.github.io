---
const { src, title } = Astro.props;
---

<img
    class="w-40 h-40 print:w-20 print:h-20 ring-4 ring-primaryLight dark:ring-primary rounded-full bg-gray-900 bg-opacity-10 mb-2 ml-12"
    src={src}
    alt={title}
/>
